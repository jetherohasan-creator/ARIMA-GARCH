# Core scientific stack
pandas>=2.0
numpy>=1.24
scipy>=1.10

# Time-series modelling
statsmodels>=0.14
arch>=6.0            # ARIMA-GARCH (ARX mean + GARCH conditional variance)
pmdarima>=2.0        # auto_arima order selection (optional: grid-search fallback)

# Regression / ML baseline
scikit-learn>=1.3

# IO & reporting
openpyxl>=3.1        # read the source .xlsx
matplotlib>=3.7
tabulate>=0.9        # markdown tables in report.md
